<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../partials/head %>
</head>
<body class="container">

	<header>
		<% include ../partials/header %>
        <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js">  </script>
        <script>
        $(document).ready(function(){
            var user;
            $("#save").click(function(){
            name=$("#inputName").val();
            $.post("/edit/<%=sensor.id%>",{name: name}, function(data){
                //alert("Name was changed!");
                location.href='/';
            });
            });
        });
        </script>
	</header>

	<main>
        <div class="col-md-8">
            <div class="form-group row">
                <label class="col-sm-2 col-form-label">ID</label>
                <div class="col-sm-10">
                <p class="form-control-static"><%=sensor.id%></p>
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-2 col-form-label">Type</label>
                <div class="col-sm-10">
                <p class="form-control-static"><%=sensor.type%></p>
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-2 col-form-label">Status</label>
                <div class="col-sm-10">
                <p class="form-control-static"><%=sensor.status%></p>
                </div>
            </div>
            
            <div class="form-group row">
                <label for="inputName" class="col-sm-2 col-form-label">Name</label>
                <div class="col-sm-10">
                <input type="text" class="form-control" id="inputName" value="<%=sensor.name%>">
                </div>
            </div>
            <button type="button" class="btn btn-sm btn-primary" id="save">Save</button> </td>
            <button type="button" class="btn btn-sm btn-danger" onclick="location.href='/';">Cancel</button> </td>
        </div>
    </main>

	<footer>
		<% include ../partials/footer %>
	</footer>
	
</body>
</html>